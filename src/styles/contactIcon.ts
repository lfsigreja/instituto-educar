import styled from 'styled-components';

export const ContactIcon = styled.a`
  height: 105px;
  width: 105px;
  transition: all .6s ease-in-out;
    &:hover {
      transform: scale(1.3);
      cursor: pointer;
    }
`;
